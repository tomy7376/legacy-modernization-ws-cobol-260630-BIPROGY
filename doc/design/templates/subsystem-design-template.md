# サブシステム設計書

> **記入ガイド**: `{...}` の部分を埋めてください。サブシステム1つにつき本ファイルをコピーして使います。
> コピー例: `cp doc/design/templates/subsystem-design-template.md doc/design/subsystems/{NN}-{name}-design.md`
> 既存のAS-IS仕様（[specs-asis/](../specs-asis/)）の記述粒度に合わせること。

---

## 基本情報

| 項目 | 内容 |
|---|---|
| サブシステム名 | `{NN}-{名前}` 例: `08-account` |
| ディレクトリ | [subsystems/{NN}-{name}/](../../../subsystems/{NN}-{name}/) |
| 分類 | マスタ参照系 / トランザクション処理系 / 運用・監査系 / 共通基盤 |
| API契約 | [copy/api/{xxx}-api.cpy](../../../subsystems/{NN}-{name}/copy/api/) |
| 作成日 | {YYYY-MM-DD} |
| ステータス | 起草 / レビュー中 / 確定 |

---

## 1. 処理概要

### 1.1 目的

{このサブシステムが担う業務機能を1〜3文で記述。例: 口座番号をキーに口座マスタを参照・存在確認し、休眠日を更新する。}

### 1.2 位置づけ・依存関係

| 区分 | 対象 | 内容 |
|---|---|---|
| 上流（呼び出し元） | `{サブシステム/ジョブ名}` | {どこから呼ばれるか} |
| 下流（呼び出し先） | `{サブシステム/共通部品}` | {何を呼び出すか。例: 21-audit へ監査記録} |
| 参照データ | `{ISAMファイル / PGテーブル}` | {一次データの所在} |

### 1.3 構成プログラム

| Program-ID | ファイル | 機能 | 主要PARAGRAPH |
|---|---|---|---|
| `{PROG-ID}` | [src/{file}.cob](../../../subsystems/{NN}-{name}/src/) | {機能概要} | `MAIN-LOGIC` ほか |

### 1.4 起動方式

| 項目 | 内容 |
|---|---|
| 起動形態 | オンライン（CALL）/ バッチ / タイマー（systemd） |
| 実行契機 | {例: API要求ごと / 日次バッチ / 月次} |
| 多重度・冪等性 | {例: 冪等。リトライ可 / 排他制御あり} |

---

## 2. 処理詳細

### 2.1 処理フロー

```
{処理の流れを擬似コード・箇条書きで記述。例:}
1. 入力パラメータ検証（{IN-FIELD} の桁数・形式）
2. 索引ファイル/テーブルを主キーで READ
3. ステータス判定（該当なし→04 / I/O失敗→12）
4. 出力エリアへ項目セット
5. （更新系の場合）WRITE/REWRITE と監査記録
```

### 2.2 主要ロジック・業務ルール

| # | ルール/分岐 | 内容 |
|---|---|---|
| 1 | `{条件}` | {振る舞い。例: ステータス=`D`（休眠）なら休眠日を必須チェック} |
| 2 | `{条件}` | {振る舞い} |

### 2.3 戻り値コード

| コード | 意味 | 発生条件 |
|---|---|---|
| `00` | 正常 | {条件} |
| `04` | 該当なし | {条件} |
| `08` | 入力不正 | {条件} |
| `10` | EOF（順次処理時） | {条件} |
| `12` | I/O失敗 | {条件} |
| `16` | 致命的エラー | {条件} |

> サブシステム固有のコードがあれば行を追加。詳細は [specs-asis/00-overview.md](../specs-asis/00-overview.md) の共通規約を参照。

### 2.4 排他・トランザクション制御

{ファイルロック/PGトランザクション境界、ロールバック方針、監査記録のタイミングなど。なければ「なし」。}

### 2.5 エラー処理・ログ

| 事象 | 処理 | ログ出力 |
|---|---|---|
| `{例: 索引オープン失敗}` | {例: 戻り値12でリターン} | [shared/copy/shared-log-api.cpy](../../../shared/copy/shared-log-api.cpy) 経由 |

---

## 3. 入力インターフェース

### 3.1 入力パラメータ（呼び出し時）

API契約: [copy/api/{xxx}-api.cpy](../../../subsystems/{NN}-{name}/copy/api/)

| COBOLフィールド名 | PIC | 必須 | 説明 | 制約・取り得る値 |
|---|---|---|---|---|
| `{IN-FIELD-1}` | `{PIC定義}` | ✓ | {説明} | {例: 13桁数字 / 範囲} |
| `{IN-OP}` | `X(1)` | ✓ | 操作区分 | {例: `L`=参照 / `A`=全件 / `R`=地域別} |

### 3.2 入力データソース

| 種別 | 名称 | 形式 | キー | 備考 |
|---|---|---|---|---|
| 索引ファイル | `{file}.idx` | 固定長{N}バイト | `{主キー}` | {副キーがあれば記載} |
| 入力ファイル | [data/{name}-seed.dat](../../../subsystems/{NN}-{name}/data/) | {形式} | — | {ロード対象等} |
| テーブル | `{table_name}` | PostgreSQL | `{pk}` | {TO-BE} |

### 3.3 前提・事前条件

- {例: 索引ファイルが LOAD 済みであること}
- {例: 上流で顧客存在確認済みであること}

---

## 4. 出力インターフェース

### 4.1 出力パラメータ（リターン時）

| COBOLフィールド名 | PIC | 説明 | 設定条件・変換ルール |
|---|---|---|---|
| `{OUT-STATUS}` | `9(2)` | 戻り値コード | 全ケースで設定 |
| `{OUT-FIELD-1}` | `{PIC定義}` | {説明} | {正常時のみ / 変換例: 9(8)→YYYYMMDD} |

### 4.2 出力データ更新（更新系の場合）

| 種別 | 名称 | 操作 | 対象項目 | 備考 |
|---|---|---|---|---|
| 索引ファイル | `{file}.idx` | REWRITE / WRITE | `{更新列}` | {例: 休眠日} |
| テーブル | `{table_name}` | UPDATE / INSERT | `{列}` | {TO-BE} |
| 監査ログ | 21-audit | 記録 | action=`{ACTION_NAME}` | {監査要件あり時} |

### 4.3 後続・事後条件

- {例: ステータスが `A`→`D` に遷移した場合、autodebit 対象から除外される}
- {例: 監査アウトボックスにイベントが1件登録される}

---

## 5. レコード定義

レコードレイアウト: [copy/private/fd-{name}.cpy](../../../subsystems/{NN}-{name}/copy/private/)

| フィールド名 | PIC | キー区分 | 説明 |
|---|---|---|---|
| `{REC-KEY}` | `{PIC定義}` | 主キー | {説明} |
| `{REC-FIELD}` | `{PIC定義}` | 副キー / — | {説明} |

---

## 6. モダナイゼーション差異メモ

| # | 項目 | AS-IS（COBOL/ISAM） | TO-BE（Java/PostgreSQL） | 対応方針 |
|---|---|---|---|---|
| 1 | `{項目}` | {例: CR-OPENED-DATE あり} | {例: 相当列なし} | {例: スコープ外} |

---

## 7. 未解決事項

| # | 項目 | 対応方針 | 担当 | 期限 |
|---|---|---|---|---|
| 1 | {項目} | {方針} | {担当} | {日時} |

---

*テンプレートバージョン: 1.0 / 参照: doc/design/specs-asis/, doc/work/modernization-brief.md*
